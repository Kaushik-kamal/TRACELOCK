# Normalise line endings in the repository so a clone on Windows and a clone
# on Linux produce identical diffs. Developed on Windows; graders may be on
# anything.
* text=auto eol=lf

# Binary assets: never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.onnx binary
*.zip binary
